@model int?
 @{
    String descriptionTarget = String.Empty;
    
    if (ViewData["descriptionTarget"] != null)
    {
        descriptionTarget = ViewData["descriptionTarget"].ToString();
    }
 }

@Html.DomainValuesListFor(m => m, descriptionTarget)